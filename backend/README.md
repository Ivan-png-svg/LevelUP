# Backend — AI Assistant

## Setup
pip install -r requirements.txt

## Configure
Edit backend/.env — paste your Gemini API key from https://aistudio.google.com/apikey

## Run
python app.py

## Production (HostIQ)
Set ALLOWED_ORIGIN to your production domain in backend/.env